# GitHub Actions для автоматичної збірки RPCSX ARMv9

Цей документ описує налаштування та використання GitHub Actions для автоматичної збірки RPCSX Android Core під архітектуру ARMv9.

## 📋 Зміст

1. [Огляд](#огляд)
2. [Сценарії активації](#сценарії-активації)
3. [Компоненти збірки](#компоненти-збірки)
4. [Виходи та артефакти](#виходи-та-артефакти)
5. [Використання](#використання)
6. [Локальна збірка](#локальна-збірка)
7. [Розв'язання проблем](#розв'язання-проблем)

---

## Огляд

Workflow `.github/workflows/build-armv9.yml` організує автоматичну збірку RPCSX для архітектури ARMv9 з оптимізаціями максимальної продуктивності.

### Ключові характеристики

- ✅ **Щотижневі збірки** - запускаються о 00:00 UTC кожного понеділка
- ✅ **Тригери на push** - автоматично при виявленні змін в C++ коді
- ✅ **Manual trigger** - можливість запустити збірку вручну через GitHub UI
- ✅ **LLVM v19+** - автоматичне завантаження та встановлення
- ✅ **ARMv9 оптимізації** - прапори `-O3 -march=armv9-a -mtune=cortex-x4`
- ✅ **LTO** - повна оптимізація на етапі посилання
- ✅ **Автоматичні патчі** - завантаження та застосування патчів

---

## Сценарії активації

### 1. Щотижневі автоматичні збірки

Workflow запускається автоматично кожного понеділка о 00:00 UTC.

```cron
0 0 * * 1
```

Це гарантує свіжу збірку з останніми оновленнями LLVM та патчами.

### 2. Тригери на push

Workflow активується при виявленні змін в:

- `app/src/main/cpp/**` - C++ вихідний код
- `app/build.gradle.kts` - Gradle конфігурація
- `build_armv9.sh` - скрипт локальної збірки
- `.github/workflows/build-armv9.yml` - сам workflow

### 3. Ручний запуск (workflow_dispatch)

Ви можете запустити збірку вручну через GitHub UI:

1. Перейдіть на вкладку **Actions** у репозиторії
2. Виберіть workflow **"Build RPCSX ARMv9"**
3. Натисніть **"Run workflow"**
4. Виберіть параметри:
   - **Build type**: `release` або `debug`
   - **LLVM version**: залишіть порожнім для використання останньої версії

---

## Компоненти збірки

### 1. LLVM Installation

```yaml
- name: Setup LLVM v${{ steps.llvm-version.outputs.version }}
```

Автоматично:
- Отримує останню стабільну версію LLVM (v19+)
- Завантажує бінарні файли з GitHub releases
- Встановлює в `/opt/llvm`
- Перевіряє версію та доступність інструментів

**Команда для перевірки:**
```bash
/opt/llvm/bin/clang --version
```

### 2. Android SDK Setup

Встановлює необхідні компоненти:
- Android SDK API 36
- Android NDK 29.0.13113456
- CMake 3.28.0

### 3. Patch Integration

Автоматично:
- Шукає файли патчів у `patches/` директорії
- Перевіряє застосовуваність (dry-run)
- Застосовує сумісні патчі
- Логує статус застосування

### 4. Build Flags

Оптимізації для ARMv9:

```bash
CFLAGS="-O3 -march=armv9-a -mtune=cortex-x4 -ffast-math -ftree-vectorize"
CMAKE_CXX_FLAGS_RELEASE="-O3 -march=armv9-a+sve2 -ffast-math -ftree-vectorize -funroll-loops"
```

Параметри:
- `-O3` - найвища оптимізація
- `-march=armv9-a` - цільова архітектура ARMv9
- `-mtune=cortex-x4` - оптимізація для Cortex-X4 (Snapdragon 8s Gen 3)
- `-ffast-math` - швидкі математичні операції
- `-ftree-vectorize` - векторизація циклів
- `-flto=full` - повна оптимізація на етапі посилання (LTO)

### 5. Gradle Build

```bash
./gradlew assembleRelease \
  -Pandroid.native.buildOutput=verbose \
  -DCMAKE_VERBOSE_MAKEFILE=ON \
  -Dorg.gradle.parallel=true \
  -DCMAKE_BUILD_TYPE=Release
```

### 6. Native Libraries Extraction

Вилучає `.so` файли з APK:
- Розпаковує бібліотеки з `lib/arm64-v8a/`
- Підраховує кількість файлів
- Обчислює загальний розмір

---

## Виходи та артефакти

### Генеровані файли

Workflow генерує та публікує наступні артефакти:

#### 1. **rpcsx-armv9-apk**
- **Файл**: `rpcsx-armv9-release-{SHA}.apk` або `rpcsx-armv9-debug-{SHA}.apk`
- **Розмір**: ~100-150 MB (release)
- **Утримується**: 60 днів

#### 2. **rpcsx-armv9-native-libs**
- **Файли**: Всі `.so` файли
- **Формат**: окремі файли
- **Утримується**: 60 днів

#### 3. **rpcsx-armv9-complete-{SHA}**
- **Формат**: `rpcsx-armv9-{version}-{sha}.tar.gz`
- **Вміст**:
  - APK файл
  - SHA256 та MD5 контрольні суми
  - Всі .so файли
  - Інформація про збірку (build-info.txt)
- **Розмір**: ~150-200 MB
- **Утримується**: 90 днів

#### 4. **build-info**
- **Файл**: `build-info.txt`
- **Формат**: текстовий звіт
- **Вміст**:
  - Дата збірки
  - Версія та SHA коміта
  - Інформація про компілятор (LLVM, Java, Gradle)
  - Флаги оптимізації
  - Налаштування Android SDK
  - Статус патчів

### Завантаження артефактів

Артефакти доступні на вкладці **Actions**:

1. Відкрийте **Actions** → виберіть **Build RPCSX ARMv9**
2. Виберіть потрібний запуск
3. Прокрутіть до розділу **Artifacts**
4. Натисніть на потрібний артефакт для завантаження

---

## Використання

### GitHub UI

#### Запуск workflow

1. Перейдіть на **Actions**
2. Натисніть **"Build RPCSX ARMv9"**
3. Натисніть **"Run workflow"**
4. Виберіть:
   - **Build type**: `release` (рекомендується)
   - **LLVM version**: залишіть порожнім (використовується v19+)
5. Натисніть **"Run workflow"**

#### Моніторинг

- Статус показується в реальному часі
- Кожен крок має описання та логи
- Помилки виділяються червоним кольором

### GitHub API (CLI)

```bash
# Переглянути останні запуски
gh run list -w build-armv9.yml -L 10

# Запустити workflow вручну
gh workflow run build-armv9.yml \
  -f build_type=release \
  -f llvm_version=19.1.0

# Завантажити артефакти
gh run download {RUN_ID} -n rpcsx-armv9-complete-{SHA}
```

---

## Локальна збірка

Для локальної збірки з тими ж параметрами що й в GitHub Actions:

### Встановлення LLVM та патчів

```bash
# Встановити останню версію LLVM
./scripts/manage-llvm-and-patches.sh install-llvm

# Або встановити конкретну версію
./scripts/manage-llvm-and-patches.sh install-llvm 19.1.0

# Завантажити патчі
./scripts/manage-llvm-and-patches.sh download-patches

# Застосувати патчі
./scripts/manage-llvm-and-patches.sh apply-patches

# Повне налаштування
./scripts/manage-llvm-and-patches.sh full-setup
```

### Локальна збірка

```bash
# Простої збірка
./scripts/build-armv9-local.sh

# Очистити та зібрати
./scripts/build-armv9-local.sh --clean

# З користувацьким LLVM
./scripts/build-armv9-local.sh --llvm-path /path/to/llvm

# Без застосування патчів
./scripts/build-armv9-local.sh --skip-patches
```

### Встановлення на пристрій

```bash
# Встановити APK
adb install -r app/build/outputs/apk/release/app-release.apk

# Перевірити архітектуру пристрою
adb shell getprop ro.product.cpu.abi

# Переглянути логи
adb logcat | grep -i "armv9\|rpcsx"
```

---

## Розв'язання проблем

### Workflow не запускається

**Проблема**: Workflow не розпочинається при push

**Рішення**:
1. Перевірте, що у вас є прав на запуск workflows
2. Переконайтеся, що файл `.github/workflows/build-armv9.yml` присутній
3. Перевірте синтаксис YAML: `gh workflow validate`
4. Перевірте тригери (розділ `on:`)

### LLVM не завантажується

**Проблема**: Помилка завантаження LLVM під час збірки

**Рішення**:
1. Перевірте інтернет-з'єднання
2. Перевірте доступність GitHub releases
3. Спробуйте вказати конкретну версію LLVM
4. Використовуйте apt-версію як fallback

### Патчі не застосовуються

**Проблема**: Патчі пропускаються або викликають конфлікти

**Рішення**:
1. Перевірте формат файлу патчу (має бути в форматі unified diff)
2. Переконайтеся, що патч сумісний з поточним кодом
3. Використовуйте `git apply` з флагом `--reject` для розслідування
4. Розглядайте статус патчу в лог-файлах

### Збірка не завершується

**Проблема**: Збірка зависає або перевищує час виконання

**Рішення**:
1. Збільшіть пам'ять Gradle: `GRADLE_OPTS=-Xmx6g`
2. Зменшіть паралельні процеси: `-Dorg.gradle.workers.max=2`
3. Перевірте диспетчера пристроїв (adb)
4. Розділіть складні модулі

### Помилки компіляції C++

**Проблема**: Помилки при компіляції нативного коду

**Рішення**:
1. Перевірте версію LLVM (має бути v19+)
2. Перевірте CMake флаги
3. Переглядайте повні логи збірки
4. Спробуйте відключити LTO (`-flto=off`)
5. Перевірте сумісність патчів з вашою версією коду

### Недостатньо місця на диску

**Проблема**: Помилка "No space left on device"

**Рішення**:
1. Очистіть старі артефакти GitHub Actions
2. Видаліть локальну директорію `.gradle/`
3. Видаліть `app/build/`
4. Перевірте вільне місце: `df -h`

---

## Налаштування додатково

### Налаштування частоти щотижневих збірок

Відредагуйте `.github/workflows/build-armv9.yml`:

```yaml
on:
  schedule:
    # Змініть день та час
    - cron: '0 12 * * 3'  # Щоди в середу о 12:00 UTC
```

### Додавання додаткових оптимізацій

Відредагуйте завдання **Configure build flags**:

```bash
export CFLAGS="-O3 -march=armv9-a -mtune=cortex-x4 ..."
```

### Налаштування утримування артефактів

Змініть `retention-days` у розділі upload-artifact:

```yaml
- uses: actions/upload-artifact@v4
  with:
    retention-days: 120  # Утримувати 120 днів
```

---

## Посилання

- [GitHub Actions документація](https://docs.github.com/actions)
- [LLVM Releases](https://github.com/llvm/llvm-project/releases)
- [Android NDK](https://developer.android.com/ndk)
- [Gradle Build System](https://gradle.org/)
- [ARMv9 Architecture](https://www.arm.com/architecture/cpu/armv9)

---

**Остання оновлення**: 22 січня 2026 р.
