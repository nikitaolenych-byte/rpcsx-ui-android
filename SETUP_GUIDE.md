# Посібник з налаштування RPCSX ARMv9

## Системні вимоги

### Мінімальні вимоги
- **Процесор**: Qualcomm Snapdragon 8s Gen 3 або новіше
- **Архітектура**: ARMv9-A з підтримкою SVE2
- **RAM**: 12GB LPDDR5X
- **Сторідж**: 128GB UFS 4.0 (рекомендовано 256GB)
- **Android**: 13 або новіше
- **GPU**: Adreno 735 або новіше

### Рекомендовані вимоги
- **Процесор**: Snapdragon 8 Gen 3 / 8 Elite
- **RAM**: 16GB LPDDR5X
- **Сторідж**: 512GB UFS 4.0
- **Android**: 14 або новіше
- **Root**: Опційно (для максимальних оптимізацій)

## Встановлення

### Крок 1: Завантаження APK

```bash
# Клонування репозиторію
git clone https://github.com/RPCSX/rpcsx-ui-android.git
cd rpcsx-ui-android

# Збірка APK
./build_armv9.sh
```

Або завантажте готовий APK з [Releases](https://github.com/RPCSX/rpcsx-ui-android/releases).

### Крок 2: Встановлення на пристрій

```bash
adb install -r rpcsx-armv9.apk
```

### Крок 3: Надання дозволів

1. Відкрийте RPCSX
2. Надайте дозвіл на доступ до файлів
3. (Опційно) Надайте root доступ для максимальної продуктивності

## Перший запуск

### 1. Встановлення PS3 Firmware

Вам потрібен офіційний PS3 firmware (PUP файл):

1. Завантажте з [PlayStation Official Site](https://www.playstation.com/support/)
2. Файл має бути версії 4.90 або новіше
3. У RPCSX: Settings → Install Firmware → Select PUP file
4. Очікуйте завершення (5-10 хвилин)

### 2. Налаштування каталогів

```
/storage/emulated/0/RPCSX/
├── config/          # Конфігурація емулятора
├── dev_hdd0/        # Віртуальний HDD PS3
│   ├── game/        # Встановлені ігри
│   ├── savedata/    # Збереження
│   └── disc/        # Disc games
├── shader_cache/    # Shader кеш (автоматично)
└── firmware/        # PS3 firmware
```

### 3. Встановлення гри

**Варіант A: ISO/Folder**
1. Скопіюйте ISO або папку гри на пристрій
2. RPCSX → Load Game → Select ISO/folder
3. Гра встановиться автоматично

**Варіант B: PKG файли**
1. RPCSX → Install PKG
2. Виберіть .pkg файл
3. Очікуйте встановлення

### 4. Активація ARMv9 оптимізацій

Оптимізації вмикаються автоматично при першому запуску гри.

Перевірка в logcat:
```bash
adb logcat | grep "ARMv9"
# Повинно бути:
# RPCSX: Enabling ARMv9 optimizations for Snapdragon 8s Gen 3
# RPCSX-NCE: Initializing NCE Engine for ARMv9
# RPCSX-Fastmem: Fastmem initialized: 10 GB
```

## Налаштування для конкретних ігор

### Plants vs. Zombies: Garden Warfare

**Рекомендовані налаштування**:
```
Resolution: 720p (internal)
FSR Upscaling: Enabled → 1440p
Target FPS: 60
Frostbite Hacks: Auto (enabled)
Thread Priority: High
GPU Governor: Performance
```

**Очікувана продуктивність**: 45-60 FPS стабільно

**Файл конфігурації**:
```bash
# /storage/emulated/0/RPCSX/config/BLUS31270.ini
[Graphics]
InternalResolution=720p
EnableFSR=true
FSRQuality=Performance
TargetFPS=60

[Performance]
EnableARMv9Optimizations=true
EnableFrostbiteHacks=true
ThreadScheduler=Aggressive

[Advanced]
ShaderCacheSize=2048
AsyncShaderCompilation=true
```

### The Last of Us

**Рекомендовані налаштування**:
```
Resolution: 720p → 1080p (FSR)
Target FPS: 30 (locked)
MLLE: Disabled (не Frostbite)
```

**Очікувана продуктивність**: 28-30 FPS

### God of War III

**Рекомендовані налаштування**:
```
Resolution: 720p → 1440p (FSR Balanced)
Target FPS: 45-60 (dynamic)
Vsync: Off
```

**Очікувана продуктивність**: 40-60 FPS (залежно від сцени)

## Оптимізація продуктивності

### Базові налаштування

1. **Вимкнення фонових додатків**
```bash
# ADB команди для вимкнення фону
adb shell am force-stop com.android.chrome
adb shell am force-stop com.google.android.gms
```

2. **Performance режим**
```bash
# Увімкнення performance governor
adb shell "echo performance > /sys/devices/system/cpu/cpu7/cpufreq/scaling_governor"
```

3. **GPU Turbo** (потрібен root)
```bash
adb shell su -c "echo performance > /sys/class/kgsl/kgsl-3d0/devfreq/governor"
```

### Розширені налаштування (Root)

1. **Thermal управління**
```bash
# Відключення thermal throttling (обережно!)
adb shell su -c "echo 0 > /sys/class/thermal/thermal_zone0/mode"
```

2. **CPU частоти**
```bash
# Фіксація на максимальній частоті
for cpu in 4 5 6 7; do
    adb shell su -c "echo performance > /sys/devices/system/cpu/cpu${cpu}/cpufreq/scaling_governor"
done
```

3. **Пріоритет процесу**
```bash
# Надання вищого пріоритету RPCSX
PID=$(adb shell pidof net.rpcsx)
adb shell su -c "renice -20 -p $PID"
```

## Troubleshooting

### Гра не запускається

**Проблема**: Black screen при запуску

**Рішення**:
1. Перевірте версію firmware (має бути 4.90+)
2. Очистіть shader cache: Settings → Clear Shader Cache
3. Перезапустіть емулятор

### Низький FPS

**Проблема**: FPS нижче 20

**Рішення**:
1. Зменшіть розширення до 720p
2. Вимкніть FSR або зменшіть якість
3. Закрийте фонові додатки
4. Увімкніть Performance режим

### Графічні баги

**Проблема**: Missing textures, flickering

**Рішення** (для Frostbite ігор):
1. Увімкніть Frostbite Hacks у налаштуваннях
2. Увімкніть Write Color Buffers
3. Спробуйте MLLE mode

### Статери (stuttering)

**Проблема**: Затримки кожні кілька секунд

**Рішення**:
1. Дочекайтеся завершення shader compilation (перші 5-10 хвилин)
2. Увімкніть Async Shader Compilation
3. Збільшіть shader cache розмір

### Перегрів

**Проблема**: Пристрій сильно гріється

**Рішення**:
1. Зменшіть target FPS до 30
2. Зменшіть розширення
3. Зніміть чохол
4. Використовуйте охолодження (вентилятор)
5. Відключіть Performance режим після прогріву

## Benchmark результати

### Snapdragon 8s Gen 3

| Гра                    | FPS (720p) | FPS (1080p FSR) | FPS (1440p FSR) |
|------------------------|------------|------------------|------------------|
| PvZ: Garden Warfare    | 58-60      | 50-55            | 45-50            |
| The Last of Us         | 28-30      | 25-28            | N/A              |
| God of War III         | 55-60      | 45-50            | 40-45            |
| Uncharted 2            | 50-55      | 40-45            | 35-40            |

### Snapdragon 8 Gen 3

| Гра                    | FPS (720p) | FPS (1080p FSR) | FPS (1440p FSR) |
|------------------------|------------|------------------|------------------|
| PvZ: Garden Warfare    | 60         | 60               | 55-60            |
| The Last of Us         | 30         | 30               | 28-30            |
| God of War III         | 60         | 58-60            | 50-55            |
| Uncharted 2            | 60         | 55-60            | 48-52            |

## Корисні команди

### Моніторинг FPS
```bash
adb logcat | grep "FPS:"
```

### Перевірка CPU частоти
```bash
adb shell cat /sys/devices/system/cpu/cpu7/cpufreq/scaling_cur_freq
```

### Температура
```bash
adb shell cat /sys/class/thermal/thermal_zone0/temp
```

### Використання RAM
```bash
adb shell dumpsys meminfo net.rpcsx
```

### Shader cache статистика
```bash
adb logcat | grep "ShaderCache"
```

## Додаткові ресурси

- **Discord**: [RPCSX Community](https://discord.gg/rpcsx)
- **Wiki**: [GitHub Wiki](https://github.com/RPCSX/rpcsx-ui-android/wiki)
- **Bug Reports**: [Issues](https://github.com/RPCSX/rpcsx-ui-android/issues)

## FAQ

**Q: Чи потрібен root?**
A: Ні, але з root доступом можна досягти кращої продуктивності через вимкнення thermal throttling та підвищення пріоритету процесу.

**Q: Скільки займає місця shader cache?**
A: Зазвичай 1-3GB на гру після кількох годин гри.

**Q: Чи можна грати онлайн?**
A: Ні, PSN не підтримується.

**Q: Чи працює на Dimensity/Exynos?**
A: Цей форк оптимізований виключно під Snapdragon з ARMv9. На інших чіпах може працювати повільніше.

**Q: Скільки заряду споживає?**
A: При активній грі ~8-12% за годину (залежно від батареї пристрою).

---

**Успішної гри! 🎮**
